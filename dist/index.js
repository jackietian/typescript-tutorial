console.log('hello world!');
const hello = () => {
    return 'Hello';
};
let foo = 23;
const style = 'bold';
const person = {
    first: 'jeff',
    last: 'bezos',
    gender: 'male',
};
const pow = (x, y) => {
    return Math.pow(x, y).toString();
};
pow(5, 10);
const arr = [];
arr.push(1);
